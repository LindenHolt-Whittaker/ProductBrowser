/**
 * Shared animation duration constants
 * Keep these values in sync with their SCSS counterparts
 */

export const ANIMATION_DURATIONS = {
  // Keep in sync with $transition-duration in ProductDetail.scss
  DRAWER_TRANSITION: 300, // milliseconds
} as const;